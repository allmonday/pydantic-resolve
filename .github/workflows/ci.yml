name: CI

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.10
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: cache poetry install
        uses: actions/cache@v4
        with:
          path: ~/.local
          # Include Python version to avoid reusing a Poetry self-venv built for a different interpreter (e.g. 3.8 -> missing libpython3.8.so)
          key: poetry-1.4.0-0-py310

      - name: Install Poetry
        uses: snok/install-poetry@v1
        with:
          version: 1.4.0
          virtualenvs-create: true
          virtualenvs-in-project: true

      - name: cache deps
        id: cache-deps
        uses: actions/cache@v4
        with:
          path: .venv
          key: pydeps-${{ hashFiles('**/poetry.lock') }}

      - name: Install Dependencies
        run: poetry install --no-interaction --no-root
        if: steps.cache-deps.outputs.cache-hit != 'true'

      - run: poetry install --no-interaction
      - run: pip uninstall pydantic
      - run: pip install pydantic==2.11.7
      - run: pip install pydantic_core==2.33.2

      - name: Test with pytest
        run: poetry run pytest tests/pydantic_v2 tests/common
